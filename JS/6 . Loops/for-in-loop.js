// for in 
const scores = {
    maths: 85,
    science: 90,
    english: 80,
    computer: 100,
};
for (a in scores) {
    console.log(a); // prints keys of scores object
}


// for in loop is used to iterate over enumerable objects like objects, arrays, strings, etc.